---
title: Getting Started
description: Install and set up starlight-copy-button in your Starlight docs site
---

`starlight-copy-button` adds an accessible, polished copy button next to each docs page title in Starlight.

## Installation

import { Steps, Tabs, TabItem } from '@astrojs/starlight/components';

<Steps>

1. Install `starlight-copy-button` from GitHub:

	<Tabs>
		<TabItem label="pnpm">

		```sh
		pnpm add github:dionysuzx/starlight-copy-button
		```

		</TabItem>
		<TabItem label="npm">

		```sh
		npm install github:dionysuzx/starlight-copy-button
		```

		</TabItem>
		<TabItem label="bun">

		```sh
		bun add github:dionysuzx/starlight-copy-button
		```

		</TabItem>
	</Tabs>

2. Add the plugin to your Starlight [`plugins`](https://starlight.astro.build/reference/configuration/#plugins) config:

   ```diff lang="js"
   // astro.config.mjs
   import starlight from '@astrojs/starlight'
   import { defineConfig } from 'astro/config'
   +import starlightCopyButton from 'starlight-copy-button'

   export default defineConfig({
     integrations: [
       starlight({
         title: 'My Docs',
   +      plugins: [starlightCopyButton()],
       }),
     ],
   })
   ```

3. Start your docs site in dev mode and open any docs page:

	<Tabs>
		<TabItem label="pnpm">

		```sh
		pnpm dev
		```

		</TabItem>
		<TabItem label="npm">

		```sh
		npm run dev
		```

		</TabItem>
		<TabItem label="bun">

		```sh
		bun run dev
		```

		</TabItem>
	</Tabs>

</Steps>

### What gets added automatically

The plugin:

- Injects client behavior to copy full page Markdown to the clipboard.
- Adds `starlight-copy-button/styles.css` to your Starlight `customCss`.
- Skips pages that use Starlight's hero title layout.
